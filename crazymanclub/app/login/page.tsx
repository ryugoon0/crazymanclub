"use client";
import {useState} from "react";import Link from "next/link";import {useRouter} from "next/navigation";import {supabase} from "@/lib/supabase";
export default function Login(){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[msg,setMsg]=useState("");const router=useRouter();async function login(){const{error}=await supabase.auth.signInWithPassword({email,password});if(error)setMsg(error.message);else router.push("/upload")}return <main className="wrap"><section className="header"><div><h1>로그인</h1><p>로그인 후 스크린샷 업로드가 가능합니다.</p></div><div className="nav"><Link href="/">홈</Link><Link href="/signup">회원가입</Link></div></section><section className="panel"><div className="form"><label>이메일</label><input value={email} onChange={e=>setEmail(e.target.value)}/><label>비밀번호</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)}/><button className="primary" onClick={login}>로그인</button>{msg&&<div className="notice">{msg}</div>}</div></section></main>}
